# eslint-config-goy

> Eslint config for GoyFE

## Install

``` bash
$ npm i eslint-config-goy -D
# OR
$ yarn add eslint-config-goy -D
```

## Config

Config in `.eslintrc.js`:

``` js 
module.exports = {
  extends: [
    'goy'
  ],

  rules: {
    // Override rules
  }
}
```
